# This is a list of files and directories that should be excluded from version control.
# The .vscode directory contains settings specific to the Visual Studio Code editor and is not necessary for the code to run.
# The __pycache__ directory contains byte-compiled Python files that are generated at runtime and can be safely ignored.
# The *.nsys-rep and *.npy files are binary files generated by certain Python libraries and are not human-readable.

# Exclude the following files and directories:
__pycache__/
.vscode/
*.nsys-rep
*.npy
